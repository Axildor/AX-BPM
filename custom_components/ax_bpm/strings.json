{
  "issues": {
    "no_tempo_analyzer": {
      "title": "AX BPM: no local tempo analyzer available",
      "description": "AX BPM is tracking {entry_title}. Deezer metadata covers most tracks, but tracks where Deezer reports bpm: 0 need local audio analysis. No audio decoder is available for the built-in NumPy tempo estimator, so those tracks will resolve to unknown.\n\nTo fix: install one of the supported decoders (miniaudio, soundfile, or an ffmpeg binary) in the Home Assistant environment, or install the AX BPM Analyzer add-on for aubio-grade accuracy."
    }
  },
  "config": {
    "step": {
      "user": {
        "title": "AX BPM Configuration",
        "description": "Track the tempo (BPM) of the currently playing media. All network requests are anonymous — no API key required. {analyzer_status}",
        "data": {
          "media_player": "Media Player",
          "octave_disambiguation": "Octave disambiguation",
          "analyzer_url": "Analyzer URL (optional, overrides auto-detect)",
          "analyzer_api_token": "Analyzer API token (optional)"
        },
        "data_description": {
          "octave_disambiguation": "Correct beat-tracker octave errors. 'Genre + mood' needs the AX BPM Analyzer add-on; it degrades to genre-only when the analyzer is unreachable.",
          "analyzer_url": "Leave empty to auto-detect the analyzer (Supervisor discovery, then homeassistant.local:8099). The analyzer provides aubio-grade tempo + mood analysis.",
          "analyzer_api_token": "Shared secret set in the AX BPM Analyzer add-on options. Sent as a Bearer token on /analyze only. Leave empty if the add-on has no token set."
        }
      }
    },
    "error": {
      "unknown": "Unexpected error"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "AX BPM Options",
        "description": "Update your AX BPM configuration. {analyzer_status}",
        "data": {
          "media_player": "Media Player",
          "octave_disambiguation": "Octave disambiguation",
          "analyzer_url": "Analyzer URL (optional, overrides auto-detect)",
          "analyzer_api_token": "Analyzer API token (optional)"
        },
        "data_description": {
          "octave_disambiguation": "Correct beat-tracker octave errors. 'Genre + mood' needs the AX BPM Analyzer add-on; it degrades to genre-only when the analyzer is unreachable.",
          "analyzer_url": "Leave empty to auto-detect the analyzer (Supervisor discovery, then homeassistant.local:8099). The analyzer provides aubio-grade tempo + mood analysis.",
          "analyzer_api_token": "Shared secret set in the AX BPM Analyzer add-on options. Sent as a Bearer token on /analyze only. Leave empty if the add-on has no token set."
        }
      }
    }
  },
  "selector": {
    "octave_disambiguation": {
      "options": {
        "off": "Off",
        "genre_only": "Genre only",
        "genre_mood": "Genre + mood"
      }
    }
  }
}
